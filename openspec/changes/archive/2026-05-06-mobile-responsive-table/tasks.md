## 1. CSS Media Query

- [x] 1.1 Add a `@media (max-width: 600px)` block to `styles/style.css`
- [x] 1.2 Set `body` padding to `8px` inside the media query
- [x] 1.3 Set `th, td` padding to `8px 6px` inside the media query
- [x] 1.4 Set `th` font-size to `11px` inside the media query

## 2. Verify

- [x] 2.1 Open the app in browser DevTools mobile view (e.g. 375px width) and confirm no horizontal scroll
- [x] 2.2 Confirm all 4 columns (Fecha, Dosis, Brazo, Aplicada) are visible without scrolling
- [x] 2.3 Confirm add/delete buttons remain accessible at the bottom
